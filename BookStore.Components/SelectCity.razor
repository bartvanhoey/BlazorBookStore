@inherits SelectCityBase

<select class="form-control col-3" @onchange="OnChangeEvent">
    @if (@Cities != null)
    {
        <option value="">select city..</option>
        @foreach (var city in Cities)
        {
            <option value="@city">@city</option>
        }
    }
</select>
