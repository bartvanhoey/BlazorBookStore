@page "/index"
@inherits IndexBase

@* <AuthorizeView>
    <Authorized>
        <p>Welcome, @context.User.Identity.Name</p>
    </Authorized>
    <NotAuthorized>
        <p>You are not signed in!</p>
    </NotAuthorized>
</AuthorizeView>

<AuthorizeView Roles="Publisher">
    <Authorized>
        <p>@context.User.Identity.Name, Your are a Publisher</p>
    </Authorized>
    <NotAuthorized>
        <p>You are not a publisher!</p>
    </NotAuthorized>
</AuthorizeView> *@

@if (IsAuthenticated)
{
    <h1><b>@user.Identity.Name</b> is logged in!!!!</h1>  
    <p>Is Publisher: @IsPublisher</p>  
    <p>Is Senior Employee: @IsSeniorEmployee</p>  
}
else
{
    <h1><b>User is logged out</b></h1>    
}